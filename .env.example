# Optional: root holding the per-EHR sample export folders (epic/, ecw/, nextgen/, iknowmed/).
# Defaults to sandbox_dashboard/sample_outputs. Override only if you keep the exports elsewhere.
# SANDBOX_ROOT=/Users/saipro05/sandbox_dashboard/sample_outputs

# DASHBOARD_FLASK_SECRET=
